testbench alu_cpm {
    sig clk

    fun tick_clock() {
        clk = 1
        $silent_tick() // tick without capturing signals
        clk = 0
        $tick()
    }
    alu alu
    test myTest {
        alu.a = 6
        alu.b = 5
        alu.alufn = b110111
        clk = 0 // initialize the value
        $tick() // capture initial state

        // test goes here
    }
}