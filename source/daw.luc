testbench daw {
    sig clk

    fun tick_clock() {
        clk = 1
        $silent_tick() // tick without capturing signals
        clk = 0
        $tick()
    }
    alu alu
    test myTest {
        clk = 0 // initialize the value
        alu.a =b11111111
        alu.b = 2
        alu.alufn = b110111
        $tick() // capture initial state

        // test goes here
    }
}